import  { StyleSheet } from 'react-native'

export default StyleSheet.create({
    container: {
        flex: 1,
        flexDirection: "row",
      },
      halfHeight: {
        flex: 0.2,
        height:50
      },

});